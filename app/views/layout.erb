<!DOCTYPE html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>KanbanBoard</title>

    <link rel="stylesheet" href="/stylesheets/main.css">
    
    

  </head>
<body>

  <% if request.path != "/login" && request.path != "/signup" %>
    <%# If the page isn't login or sign up show layout & yield %>
    <h1><%= current_user.username %></h1>
    <nav>
      <a href="/logout">Log Out</a>
      <a href="/projects/new">Start A Project</a>
      <form action="/users/<%= current_user.id %>/delete" method="post">
          <input type="hidden" name="_method" value="delete">
          <button type="submit">Delete Account</button>
      </form>
    </nav>
    
    <%= yield %>
  
  <% else %>
    <%# If the page is login or sign up yield only specific page %>
    <%= yield %>
  <% end %>
        

  <%# Footer %>
  <footer class="container-fluid">
  <small>&copy; 2020 <strong>KanbanBoard</strong></small>
  </footer>

</body>
</html>
