<!DOCTYPE html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>KanbanBoard</title>
    <link rel="stylesheet" href="/stylesheets/main.css">
  </head>
<body>

<% if request.path != "/login" && request.path != "/signup" %>
<%# If the page isn't login or sign up show layout & yield %>
    <header id="layout__header">
      <% if @project %>
        <h1><%= @project.title %></h1>
      <% end %>
    <nav>
        <a class="header__controls" href="/users/<%= current_user.id %>"><%= current_user.username %></a>
        <a class="header__controls" id="header__logout" href="/logout">Log Out</a><br>
        <a class="header__controls" href="/users/<%= current_user.id %>">My Projects</a>
        <a class="header__controls" id="header__new-project" href="/projects/new">New Project</a>
        <% if @project %>
          <a class="header__controls" id="header__new-project" href="/projects/<%= @project.id %>/edit">Edit Project</a>
          <form action="/project/<%= @project.id %>/delete" method="post">
            <input type="hidden" name="_method" value="delete">
            <input type="submit" value="Delete Project?">
          </form>
          <a class="header__controls" id="header__new-project" href="/projects/<%= @project.id %>/edit">Delete Project</a>
        <% end %>
        <form action="/users/<%= current_user.id %>/delete" method="post">
            <input type="hidden" name="_method" value="delete">
            <button class="header__controls" id="header__delete-account" type="submit">Delete Account</button>
        </form>
      </nav>
    </header>
    
    <%= yield %>
  
  <% else %>
    <%# If the page is login or sign up yield only specific page %>
    <%= yield %>
  <% end %>
        

  <%# Footer %>
  <footer class="container-fluid">
  <small>&copy; 2020 <strong>KanbanBoard</strong></small>
  </footer>

</body>
</html>
